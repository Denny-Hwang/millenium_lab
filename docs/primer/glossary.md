# Glossary

> Mathematical terms and rhetorical phrases. Living document — extend
> as new terms surface in attempts, candidates, reviews, or external
> reading. Each entry should carry (a) a short definition, (b) a usage
> example, and ideally (c) a pointer to where the term first appeared
> in this repository or in cited external work.

---

## 1. Statement types

TODO: theorem, lemma, proposition, corollary, claim, fact,
observation, remark, conjecture — what each connotes about
*author confidence*, *centrality to the argument*, and *proof
obligation*.

## 2. Proof-internal vocabulary

TODO: hypothesis, premise, assumption, axiom; necessary vs
sufficient; *if and only if* and its shorthand "iff"; "vacuously
true"; "trivially"; "without loss of generality (WLOG)";
"by symmetry"; "by abuse of notation"; "modulo X"; "up to X"; "for
a generic X"; "for X sufficiently large / small"; "in general
position".

## 3. Latin and stock phrases

TODO: *mutatis mutandis*, *a fortiori*, *a priori* / *a posteriori*,
*ad absurdum*, *ceteris paribus*, *cf.*, *q.v.*, *viz.*, *ibid.*,
*pace*, *passim*. Note which are still in active use in modern
papers and which read as archaic.

## 4. Hedging and emphasis

TODO: "It suffices to show ...", "It is enough to ...", "We may
assume ...", "Note that ...", "Observe that ...", "Indeed, ...",
"In fact, ...", "Recall that ...", "One checks that ...", "It is
easy to see that ..." (and why this last phrase is treacherous).

## 5. Existence, uniqueness, and modality

TODO: "there exists", "there is", "there exists a unique", "for
some", "for any" vs "for all" (informal vs formal use); "essentially
unique"; "canonically isomorphic"; "natural" (the technical sense
from category theory vs the colloquial sense).

## 6. Sizes and growth

TODO: $O(\cdot), \Omega(\cdot), \Theta(\cdot), o(\cdot), \omega(\cdot)$;
"polynomial", "sub-exponential", "quasi-polynomial"; "at most",
"at least", "of order"; "asymptotically".

## 7. Repository-specific terms

TODO: terms introduced inside this repository that are not yet
standard externally. Cite the attempt or bridge of origin.

Examples to fill in:

- `H_AC⁰` — the AC⁰ pilot hypothesis introduced in
  `attempts/03-p-vs-np/A002-.../`; refined in A004.
- `lethal-if-unresolved` — gap-severity label used in result.md
  files; defined in
  [`../methodology/`](../methodology/).
- `succinct program description` — input-representation convention
  used in `L11'` of A004.

---

## Open questions / TODO

- Decide whether to alphabetize within sections or keep them in
  logical order; current draft prefers logical order.
- Add a cross-reference column to each entry once we have more than
  ~20 entries.
