# Main Proof — Natural-Language Body

This file holds the natural-language proof body. Every lemma is split
out into `lemmas/L##.md`; this file only cites them.

> ⚠️ If you place "QED" or "proof complete" in this file, CI
> (`claim-integrity.yml`) requires:
> - `main-proof.lean` exists,
> - `main-proof.lean` builds,
> - the candidate's `verification_status.L3` is `pass`.
>
> If those conditions fail, the PR is blocked automatically.

---

## Theorem

> TODO: conclusion statement. Must match `claim.md`.

## Proof

TODO: natural-language proof. Cite lemmas as `[L##]`.

(Do not write "QED" yet. It is allowed only after L3 passes.)
